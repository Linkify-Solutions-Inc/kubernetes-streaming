apiVersion: v1
kind: ServiceAccount
metadata:
  name: transcode-worker
# Module 7 attaches the IAM role that can read raw/ and write hls/.
# Module 14 keeps this ServiceAccount and hands it to the KEDA ScaledJob
# pods instead of the Deployment below, so the association survives.
